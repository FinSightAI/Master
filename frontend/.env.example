# Sentry — fill from sentry.io project → Settings → Client Keys (DSN)
NEXT_PUBLIC_SENTRY_DSN=
# Required only for source-map upload during `next build` on Vercel
SENTRY_AUTH_TOKEN=
SENTRY_ORG=
SENTRY_PROJECT=
